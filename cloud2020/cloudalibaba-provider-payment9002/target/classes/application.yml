server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    # 配置 nacos 地址
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址

# 暴露端口
management:
  endpoints:
    web:
      exposure:
        include: '*'